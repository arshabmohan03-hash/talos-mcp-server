# Web framework
fastapi==0.136.3
uvicorn[standard]==0.34.0

# AI
cerebras_cloud_sdk==1.67.0
groq==1.4.0

# Scanner / HTTP / crypto
httpx==0.28.1
dnspython==2.7.0
cryptography==43.0.3

# Config / models
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.0.1

# Auth + Firestore
firebase-admin==6.5.0

# Brute-force ML + numerical core (numpy is imported directly in app/resources.py for OCR;
# pinned to the version the pickled model + onnxruntime/opencv are verified against)
numpy==2.4.6
scikit-learn==1.5.0
joblib==1.4.2
pandas==2.2.2
matplotlib==3.10.8

# Extra security-tool libraries (codext encodings, recon, OSINT, forensics, crypto)
codext==1.16.1
netaddr==1.3.0
phonenumbers==9.0.31
dnstwist==20250130
python-whois==0.9.6
ipwhois==1.3.0
zxcvbn==4.5.0
yara-python==4.5.4
hashID==3.1.4
wafw00f==2.4.2
beautifulsoup4==4.14.3
lxml==6.1.1
passlib==1.7.4
bcrypt==5.0.0
argon2-cffi==25.1.0
sympy==1.14.0

# Resource library (PDF -> page-numbered markdown; multipart = file uploads)
pypdf==5.1.0
python-multipart==0.0.20

# OCR for scanned / image-only PDFs (pure-pip — NO system binary needed; see INSTALL_BINARIES.md)
pypdfium2==5.9.0                  # render PDF pages to images
pillow==12.2.0                    # image handling
rapidocr-onnxruntime==1.4.4       # ONNX OCR engine (bundles models, runs offline)
pytesseract==0.3.13               # optional fallback — only if a 'tesseract' binary is present

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
